<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main"
    android:padding="20dp">

    <!-- Logo Image -->
    <ImageView
        android:id="@+id/logo_image"
        android:layout_width="273dp"
        android:layout_height="170dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:src="@drawable/btn_bg"
        tools:ignore="ContentDescription,ImageContrastCheck" />

    <!-- Email Field -->
    <EditText
        android:id="@+id/email_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo_image"
        android:layout_marginTop="50dp"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:textColorHint="@color/white"
        tools:ignore="Autofill,HardcodedText,TextSizeCheck,TextContrastCheck,TouchTargetSizeCheck,VisualLintTextFieldSize" />

    <!-- Password Field -->

    <!-- Forgot Password Button -->
    <EditText
        android:id="@+id/password_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/email_field"
        android:layout_marginTop="24dp"
        android:hint="Password"
        android:inputType="textPassword"
        android:textColorHint="@color/white"
        tools:ignore="Autofill,HardcodedText,TextSizeCheck,TextContrastCheck,TouchTargetSizeCheck,VisualLintTextFieldSize" />

    <Button
        android:id="@+id/forgot_password_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/password_field"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="10dp"
        android:text="Forgot Password?"
        android:textColor="@color/teal"
        tools:ignore="HardcodedText,TextSizeCheck,TextContrastCheck" />

    <!-- Sign in Button -->
    <Button
        android:id="@+id/sign_in_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/forgot_password_button"
        android:layout_marginTop="30dp"
        android:background="@color/teal"
        android:text="Sign in"
        android:textColor="@color/purple_200"
        tools:ignore="HardcodedText,TextSizeCheck,TextContrastCheck,VisualLintButtonSize" />

    <!-- Register Button -->
    <Button
        android:id="@+id/register_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sign_in_button"
        android:layout_marginTop="10dp"
        android:background="@color/teal"
        android:text="Register"
        android:textColor="@color/main"
        tools:ignore="HardcodedText,TextSizeCheck,TextContrastCheck,VisualLintButtonSize" />

</RelativeLayout>
